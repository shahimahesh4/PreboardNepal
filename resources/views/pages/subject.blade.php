<x-layouts.app :title="$subject->name"><div class="page-container"><a class="breadcrumb" href="{{ route('library') }}">← Study library</a><div class="page-heading"><div><p class="eyebrow">{{ $subject->curriculum }}</p><h1>{{ $subject->name }}, one idea at a time.</h1><p>{{ $subject->description }}</p></div><span class="subject-icon large tone-{{ $subject->color }}"><x-preboard-icon :name="$subject->symbol"/></span></div><div class="chapter-strip">@foreach($subject->chapters as $chapter)<div><span>{{ str_pad($loop->iteration,2,'0',STR_PAD_LEFT) }}</span><strong>{{ $chapter->title }}</strong><small>{{ $chapter->documents_count }} resources</small></div>@endforeach</div><div class="section-heading"><h2>Build your understanding</h2></div><div class="resource-grid">@forelse($documents as $document)<x-resource-card :document="$document"/>@empty<div class="empty-state"><h3>New resources are being prepared.</h3><a href="{{ route('library') }}">Explore other subjects →</a></div>@endforelse</div><div class="section-heading"><h2>Put it into practice</h2></div><div class="resource-grid">@foreach($exams as $exam)<x-exam-card :exam="$exam"/>@endforeach</div></div></x-layouts.app>
