@props(['document','interactive'=>false,'saved'=>false])
<article class="resource-card"><a href="{{ route('resources.show',$document) }}" class="resource-cover tone-{{ $document->chapter->subject->color }}" tabindex="-1" aria-hidden="true"><span class="tiny-label">{{ str_replace('_',' ',$document->type) }}</span><span class="cover-art"><span class="paper-top">PREBOARD / {{ strtoupper(mb_substr($document->chapter->subject->name,0,3)) }}</span><span class="paper-title">{{ $document->chapter->title }}</span><i></i><i></i><i></i><i></i></span><span class="cover-number">{{ str_pad($document->id,2,'0',STR_PAD_LEFT) }}</span></a><div class="resource-content"><div class="resource-meta"><span>{{ $document->chapter->subject->name }} · {{ $document->language }}</span><span class="access-label">{{ $document->is_free?'Free':'Study Pass' }}</span></div><h3><a href="{{ route('resources.show',$document) }}">{{ $document->title }}</a></h3><p>{{ $document->description }}</p><div class="resource-bottom"><span><x-preboard-icon name="clock"/>{{ $document->reading_minutes }} min read</span>@if($interactive)<button class="bookmark-button" wire:click="toggleSave({{ $document->id }})" wire:loading.attr="disabled" aria-label="{{ $saved?'Remove from':'Add to' }} saved: {{ $document->title }}" aria-pressed="{{ $saved?'true':'false' }}"><x-preboard-icon name="bookmark"/>{{ $saved?'Saved':'Save' }}</button>@else<a class="read-link" href="{{ route('resources.show',$document) }}">Read notes →</a>@endif</div></div></article>
