@props(['exam'])
<article class="exam-card"><div class="flex-between"><span class="subject-icon tone-{{ $exam->subject->color }}"><x-preboard-icon :name="$exam->subject->symbol"/></span><span class="badge">Free practice</span></div><p class="eyebrow">{{ $exam->subject->name }}</p><h3>{{ $exam->title }}</h3><p>{{ $exam->description }}</p><div class="exam-details"><span><x-preboard-icon name="clock"/>{{ $exam->duration_minutes }} minutes</span><span><x-preboard-icon name="document-text"/>{{ count($exam->questions) }} questions</span></div><p class="exam-note">1 mark per answer · No negative marking</p>@auth<form action="{{ route('practice.start',$exam) }}" method="post">@csrf<button class="btn btn-primary w-full">Start or resume practice <span>→</span></button></form>@else<a class="btn btn-primary w-full" href="{{ route('login') }}">Sign in to practise <span>→</span></a>@endauth</article>
