<x-layouts.app title="Your orders"><div class="page-container"><p class="eyebrow">MEMBERSHIP</p><h1>Your orders</h1>@forelse($orders as $order)<article class="panel" style="padding:24px;margin-top:16px"><a href="{{ route('billing.show',$order) }}"><strong>{{ $order->title }}</strong> · NPR {{ number_format($order->amount_paisa/100,2) }}</a><p>{{ $order->providerLabel() }} · {{ ucfirst(str_replace('_',' ',$order->status)) }} · {{ $order->created_at->format('j M Y') }}</p></article>@empty<p>You have no orders yet. Your free study resources are ready whenever you are.</p><a class="btn btn-primary" href="{{ route('library') }}">Explore the library</a>@endforelse {{ $orders->links() }}</div></x-layouts.app>
