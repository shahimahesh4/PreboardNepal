<div><div class="page-heading"><div><p class="eyebrow">MAKE IT YOURS</p><h1>Your account, your pace.</h1><p>Keep your learning profile up to date.</p></div></div>@if(session('status'))<div class="alert success" role="status">{{ session('status') }}</div>@endif<form wire:submit="save" class="panel padded account-form"><h3>Learning profile</h3><label for="account-name">Name</label><input id="account-name" wire:model="name" maxlength="100" autocomplete="name">@error('name')<p class="field-error">{{ $message }}</p>@enderror<label for="account-grade">Your grade</label><select id="account-grade" wire:model="grade"><option value="10">Grade 10 / SEE</option><option value="11">Grade 11</option><option value="12">Grade 12</option></select><small>The current foundation library is aimed at Grade 12. More coverage is planned.</small><label>Email address</label><p style="margin:0 0 20px">{{ auth()->user()->email }} <span class="badge">{{ auth()->user()->hasVerifiedEmail()?'Verified':'Not yet verified' }}</span></p><button class="btn btn-primary" wire:loading.attr="disabled">Save changes</button></form><div class="button-row"><form action="{{ route('logout') }}" method="post">@csrf<button class="btn btn-secondary">Sign out</button></form><a class="text-link" href="{{ route('password.request') }}">Password recovery</a></div></div>
